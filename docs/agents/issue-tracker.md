# Issue Tracker

Issues for this repo are tracked in **Jira**.

Specifics (project key, site URL, board, conventions) are not yet recorded — the user chose to skip filling these in during setup. Edit this file directly once known.

## For now, agent skills should

- Not assume a `gh issue create` or `.scratch/` workflow — this repo does not use either.
- When a skill needs to file or read an issue, ask the user for the Jira project key / URL rather than guessing.
- Once specifics are added below, skills should follow them without re-asking.

## Jira details (TBD)

- Site URL:
- Project key:
- Issue-creation convention:
