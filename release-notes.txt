# 20.26.3.1_GA
# CoreStation HX Agent Release notes

CoreStation HX Agent Release notes

―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
20.26.5.1_rc7
-------------
- Made c2a Dialog show on user desktop


20.25.5.1_rc6
-------------
- Made tray app exit and launch depending on session state, so that user can sign out then sign in and see the tray app


20.26.5.1_rc5
-------------
- Added functional Tray application - Reports hostname, OS version, and LAN Addresses, not APIPA Addresses
- Fixed missing handlers in old Tray app code
- Set tray app launch to use the CreateProcessAsUser call so that the service can launch the tray on users session rather than system session //TODO: Add this to the c2a Handler
- Set tray app icon to main icon for app


20.26.5.1_rc4
-------------
Focussed on bringing debian build up-to-date
- Updated changelog
- added missing handler to linux build
- Updated CMakeLists
- Added partial DHCP/Static check
- Fix debian build so it works again
- Switch Linux build Serial from ttyUSB0 to ttyS2


20.26.5.1_rc3
-------------
- Updated HX3000 check to use CPU Name, rather than NIC Presence, as not all HX3000s will have 4x NICs or E610s may be switched for something else


20.26.5.1_rc2
-------------
- Fixed wrong HWID for E610
- Made MAC Filtering use 3x pairs rather than 2 to make more vendor specific


20.26.5.1_rc1
-------------
- First implemenetation of HX3000 Support - Checks For presence of 2 E610s and 2 I226s, if statisfied, uses COM1
- Added AAEON MAC to MAC Filter


20.26.3.1_GA
------------
- Made Session state reporting more robust
- Added Hearbeat
- Adds a check for the AMT Serial Port, which will reassign it to COM4 if it is conflicting on COM3
- To aid with the above, the application will not exit if the Serial Port cannot be opened, it will just retry opening the port every 5 Seconds
- Reports full windows version ("Windows 11 Pro 25H2") rather than the 6.1s "Microsoft Windows 11 Pro", this just makes it a little more clear as Windows is always Microsoft, but Windows isnt just Pro


2025.9.1
--------
- Renamed to CoreStation HX Agent
- Add Heartbeat Message every 30 seconds to indicate Service and Node are alive
- Added NEW Linux Build, Architecture to build is determined by build OS


2025.6.1
--------
- Report username for RDP sessions
- Set username to "none" if WTS_SESSION_LOGOFF detected
- Reduced main loop time from 500 to 50 ms to enusure all session state changes are captured
- Added milli-seconds to log timestamp


2025.5.2
--------
- Switch port to COM3 to support MVP congatec COM, Advantec will need COM1 moving to COM3
- Added debug log to C:\ProgramData\ahk\node-win-app if not GA version
- Added auto EV signing to .exe file


2025.4.2
--------
- First release that pushes following on change to COM 1 : 
    - win version
    - ethernet details
    - username
    - hostname
    - session state
    - reset network adapter on network down
    